# should fail

-
    type: sql
    mode: query
    mysql: "SELECT CURDATE() AS today FROM ezcontentobject WHERE 1 = 0"
    references:
        -   identifier: ref
            attribute: results.today
